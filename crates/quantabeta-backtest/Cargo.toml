[package]
name    = "quantabeta-backtest"
version = "0.1.0"
edition = "2021"

[dependencies]
quantabeta-core = { path = "../quantabeta-core" }
rug             = { version = "1.24", features = ["rational"] }
serde           = { version = "1", features = ["derive"] }
serde_json      = "1"
sha2            = "0.10"
